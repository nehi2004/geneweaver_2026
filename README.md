# Core scientific stack
numpy>=1.24
biopython>=1.81

# GPU acceleration (needs an NVIDIA GPU + CUDA toolkit installed on your machine)
numba>=0.58

# Distributed scheduling (multi-GPU / multi-machine)
dask[distributed]>=2024.1.0

# Terminal dashboard
textual>=0.52
rich>=13.7

# Optional: real GPU telemetry (memory/util) for the dashboard
pynvml>=11.5
"# geneweaver_2026" 
